0a. familiarize yourself with the code in src/

read @implementation-plan.md and implement the single highest priority feature - only work on a SINGLE feature

ensure all tests and linting passes with bun check && bun test, then update implementation-plan.md with your progress

use git add -A and git commit -m "..." to commit your changes

git push origin main

if all tasks in the implementation-plan.md are finished, cancel the ralph loop