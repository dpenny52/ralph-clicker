0a. familiarize yourself with specs/

0b. familiarize yourself with the code in src/

read @implementation-plan.md and implement the single highest priority feature using up to 5 subagents, including anything in the out of scope / future work - that's now in scope!

ensure all tests and linting passes with bun check && bun test, then update implementation-plan.md with your progress

use git add -A and git commit -m "..." to commit your changes

git push origin main